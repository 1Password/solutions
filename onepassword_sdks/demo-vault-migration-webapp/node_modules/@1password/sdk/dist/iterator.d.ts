export declare class SdkIterable<T> implements AsyncIterable<T> {
    private readonly elements;
    constructor(elements: T[]);
    [Symbol.asyncIterator](): AsyncIterator<T>;
    private asyncIterator;
}
