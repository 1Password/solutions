<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Vault Migration Tool</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* Reset default body margins and set font */
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #2C3E50;
    }
    /* Flex container for sidebar and content layout */
    .container {
      display: flex;
      height: 100vh;
      background-color: #2C3E50;
    }
    /* Sidebar styling with gradient background */
    .sidebar {
      width: 250px;
      background: linear-gradient(180deg, #34495E 0%, #2C3E50 100%);
      color: #ECF0F1;
      padding-top: 20px;
      transition: width 0.3s;
      box-shadow: 2px 0 5px rgba(0, 0, 0, 0.3);
    }
    /* Sidebar header styling */
    .sidebar h1 {
      color: #3498DB;
      font-size: 1.8em;
      text-align: center;
      margin-bottom: 30px;
      padding: 10px;
      border-bottom: 1px solid #2C3E50;
    }
    /* Remove default list styling for sidebar menu */
    .sidebar ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    /* Style for individual sidebar list items */
    .sidebar ul li {
      padding: 15px 20px;
      border-bottom: 1px solid #2C3E50;
      transition: background 0.3s;
    }
    /* Sidebar link styling with flex alignment */
    .sidebar ul li a {
      color: #ECF0F1;
      text-decoration: none;
      font-size: 1.2em;
      display: flex;
      align-items: center;
      gap: 10px;
      transition: color 0.3s, transform 0.3s;
    }
    /* Hover and active states for sidebar links */
    .sidebar ul li a:hover,
    .sidebar ul li a.active {
      color: #3498DB;
      transform: scale(1.05);
    }
    /* Background hover effect for sidebar links */
    .sidebar ul li a:hover {
      background: rgba(52, 73, 94, 0.8);
    }
    /* Icon alignment within sidebar links */
    .sidebar ul li a i {
      width: 20px;
      text-align: center;
    }
    /* Main content area with flex layout */
    .content {
      flex-grow: 1;
      padding: 20px;
      background-color: #2C3E50;
      color: #ECF0F1;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      justify-content: flex-start; /* Align content to the top */
      align-items: center;
    }
    /* Styling for the welcome message container */
    .form-container {
      background: rgba(52, 73, 94, 0.8);
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
      max-width: 1800px;
      margin-left: auto;
      margin-right: auto;
      text-align: center;
    }
    /* Heading style within the form container */
    .form-container h1 {
      color: #3498DB;
      margin-bottom: 25px;
      font-size: 2.5em;
      text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
    }
    /* Paragraph text styling in the form container */
    .form-container p {
      font-size: 1.2em;
      color: #ECF0F1;
      margin: 0 0 20px;
    }
    /* Responsive design adjustments for smaller screens */
    @media (max-width: 768px) {
      .sidebar {
        width: 60px;
      }
      .sidebar h1 {
        font-size: 1em;
        writing-mode: vertical-rl;
        transform: rotate(180deg);
        margin: 20px 0;
      }
      .sidebar ul li {
        padding: 10px;
      }
      .sidebar ul li a {
        font-size: 0;
      }
      .sidebar ul li a i {
        font-size: 1.2em;
      }
      .content {
        padding: 10px;
      }
      .form-container {
        padding: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="sidebar">
      <h1>Vault Migration Tool</h1>
      <ul>
        <li><a href="/" class="<%= currentPage === 'welcome' ? 'active' : '' %>"><i class="fas fa-home"></i> Home</a></li>
        <li><a href="/migration" class="<%= currentPage === 'migration' ? 'active' : '' %>"><i class="fas fa-exchange-alt"></i> Vault Migration</a></li>
      </ul>
    </div>
    <div class="content">
      <div class="form-container">
        <h1>Welcome to the Vault Migration Tool</h1>
        <p>Migrate vaults between 1Password tenants with ease. Click "Vault Migration" in the sidebar to get started.</p>
      </div>
    </div>
  </div>
</body>
</html>