# Acronis Deployment Script

This deployment script is designed for use with Acronis RMM. It automatically detects whether or 
not it is being ran on a version of Windows that supports MSIX, and if so automatically uses that
version. Otherwise, the MSI will be used instead. 

## Credit

Acronis for original script.
